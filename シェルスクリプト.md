# シェルスクリプト演習

## shell-1

```bash
#!/bin/bash

SRC_FILE="/etc/httpd/conf/httpd.conf"
BK_DIR="/home/ec2-user/onishi/backup/"

NOW=$(date +"%Y%m%d%H%M%S")

BK_FILE="${BK_DIR}/httpd.conf.${NOW}.bak"

mkdir -p ${BK_DIR}

if [ ! -f ${SRC_FILE} ];then
        echo "バックアップ元ファイルがありません"
        exit 1
fi

cp "${SRC_FILE}" "${BK_FILE}"

if [ $? -eq 0 ]; then
  echo "バックアップ完了: ${BK_FILE}"
else
  echo "エラー: バックアップに失敗しました"
  exit 1
fi
```

#### 確認
```bash
ls -l onishi/backup
```

---

## shell-2

```bash
#!/bin/bash

SRC_FILE="$1"
BK_DIR="$2"

NOW=$(date +"%Y%m%d%H%M%S")

FILE_NAME=$(basename "${SRC_FILE}")

BK_FILE="${BK_DIR}/${FILE_NAME}.${NOW}.bak"


mkdir -p ${BK_DIR}

if [ ! -f ${SRC_FILE} ];then
        echo "バックアップ元ファイルがありません"
        exit 1
fi

cp "${SRC_FILE}" "${BK_FILE}"

if [ $? -eq 0 ]; then
  echo "バックアップ完了: ${BK_FILE}"
else
  echo "エラー: バックアップに失敗しました"
  exit 1
fi
```

#### 確認
```bash
ls -l /home/ec2-user/onishi/backup
```

---

## shell-3

```bash
#!/bin/bash

echo "バックアップ元は？"
read SRC_FILE

echo "バックアップ先は？"
read BK_DIR

NOW=$(date +"%Y%m%d%H%M%S")

FILE_NAME=$(basename "${SRC_FILE}")

BK_FILE="${BK_DIR}/${FILE_NAME}.${NOW}.bak"


mkdir -p ${BK_DIR}

if [ ! -f ${SRC_FILE} ];then
        echo "バックアップ元ファイルがありません"
        exit 1
fi

cp "${SRC_FILE}" "${BK_FILE}"

if [ $? -eq 0 ]; then
  echo "バックアップ完了: ${BK_FILE}"
else
  echo "エラー: バックアップに失敗しました"
  exit 1
fi
```

#### 確認
```bash
ls -l /home/ec2-user/onishi/backup
```

---

## shell-4

